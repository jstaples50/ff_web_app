import React from "react";

const TeamBadge = ({ team }) => {
  return (
    <div>
      <h3>TeamBadge Component</h3>
      {/* <p>{team.teamName}</p> */}
    </div>
  );
};

export default TeamBadge;
